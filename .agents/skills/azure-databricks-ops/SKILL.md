# Azure and Databricks operations

Use this skill for setup scripts, Bicep, Databricks SQL, and cost control.

- Treat deployments and warehouse starts as explicit, user-approved operations.
- Keep `.risk.env.local` and Foundry metadata out of git.
- Prefer `what-if` and validation commands before mutations.
- Update `docs/azure-setup.md`, `docs/cost-control.md`, and README when operational flow changes.
