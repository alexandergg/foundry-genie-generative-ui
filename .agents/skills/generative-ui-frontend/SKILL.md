# Generative UI frontend

Use this skill for `apps/web` changes.

- Keep CopilotKit runtime usage centralized through `/api/copilotkit`.
- Keep generative UI components controlled by typed props.
- Prefer deterministic chart data mapping and clear fallback UI.
- Validate with `npm run validate:web`.
