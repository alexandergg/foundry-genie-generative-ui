# Demo script

## Goal

Demonstrate that a user can ask governed business questions, approve access, receive visual answers, and ask follow-ups that reuse Foundry/session context when possible.

## Before the session

1. Run `./scripts/90-validate-uc3.sh`.
2. Start the SQL Warehouse if needed.
3. Start the local agent and web app.
4. Open <http://localhost:3000>.

## Script

### 1. First governed query

Ask:

> What is the total exposure by country in 2026-Q2?

Expected:

- Human approval card appears.
- After approval, Foundry queries Genie.
- UI renders KPIs, table, bar chart, and donut/share chart.

### 2. Conversational memory follow-up

Ask:

> From the previous result, show the top 3 countries only and explain the concentration risk.

Expected:

- The bridge should reuse the previous returned table when sufficient.
- No new governed data approval should be required for simple table reuse.

### 3. Trend / variation visual

Ask:

> Which countries explain the exposure variation between 2026-Q1 and 2026-Q2?

Expected:

- Foundry/Genie may query fresh quarter data.
- UI can render a line/area trend or metric comparison depending on the returned table.

### 4. Multi-metric comparison

Ask:

> Compare exposure, overdue balance and claims by broker segment.

Expected:

- Grouped metric comparison chart.
- KPI strip for totals.
- Business narrative with assumptions and filters.

### 5. Risk-focused drill-down

Ask:

> Break down overdue balance by risk class for 2026-Q2 and suggest where the executive review should focus.

Expected:

- Risk narrative card.
- Table and chart by risk class.
- Follow-up questions generated by the UI.

## Success criteria

- The browser never exposes cloud credentials.
- The user sees explicit approval before governed data access.
- Foundry and Genie provide real data-backed answers.
- Visual components are rendered as controlled React components.
- Follow-up questions can use conversation/session context.
